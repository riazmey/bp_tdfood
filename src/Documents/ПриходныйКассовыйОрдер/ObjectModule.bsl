
&После("ОбработкаЗаполнения")
Процедура Базар_ОбработкаЗаполнения(ДанныеЗаполнения, ТекстЗаполнения, СтандартнаяОбработка)
	
	Если РасшифровкаПлатежа.Количество() = 1 тогда
		РасшифровкаПлатежаДоговорКонтрагента = РасшифровкаПлатежа.Получить(0).ДоговорКонтрагента;
		Если НЕ ЗначениеЗаполнено(РасшифровкаПлатежаДоговорКонтрагента) тогда
			Основание = "";
		Иначе
			Основание = Строка(РасшифровкаПлатежаДоговорКонтрагента);
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры
