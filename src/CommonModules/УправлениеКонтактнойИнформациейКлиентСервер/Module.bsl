&Вместо("ОписаниеНовойКонтактнойИнформации")
Функция Базар_ОписаниеНовойКонтактнойИнформации(Знач ТипКонтактнойИнформации) Экспорт
		
	Результат = ПродолжитьВызов(ТипКонтактнойИнформации);
	
	Если НЕ Результат = Неопределено тогда
		Если ТипКонтактнойИнформации = ПредопределенноеЗначение("Перечисление.ТипыКонтактнойИнформации.Адрес") Тогда
			Результат.Вставить("latitude",  0);
			Результат.Вставить("longitude", 0);
		КонецЕсли;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции